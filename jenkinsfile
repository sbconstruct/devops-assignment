pipeline {
    agent any

    stages {
        stage("Build") {
            steps {
                dir('my-app') {
                sh "mvn -version"
                sh "mvn clean install"
                }

            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}