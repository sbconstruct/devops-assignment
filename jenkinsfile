pipeline {
    agent any

    stages {
        stage("Build") {
            steps {
                sh "cd my-app"
                sh "mvn -version"
                sh "mvn clean install"
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}